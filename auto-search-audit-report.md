# تقرير تدقيق وإصلاح البحث التلقائي عن روابط واتساب

## الحكم التنفيذي

تم إصلاح المسار البرمجي الأساسي للبحث التلقائي وتحسين موثوقيته. **النتيجة: 🟡 PARTIAL اعتماداً على فحوص الكود والبناء واختبارات الاستخراج؛ إثبات القراءة الحية من حساب واتساب وقاعدة PostgreSQL يحتاج بيئة تشغيل متصلة بحساب فعلي.**

## السبب الجذري

كان المسار اللحظي موجوداً، لكنه كان يبتلع أخطاء `AutoSearchService` داخل `WhatsAppManager`، ولا يملك Claim دائمًا للرسائل، لذلك كان من الممكن إعادة معالجة نفس الرسالة عند تداخل `messages.upsert` مع `messaging-history.set` أو عند إعادة الاتصال. كما أن مصادر الروابط كانت تقبل التكرار بلا مفتاح مصدر فريد، وكانت أعداد المجموعات قابلة للتضخم مع كل دورة فحص.

أما الفحص التاريخي فكان يعتمد على واجهة `fetchMessageHistory` إن كانت موجودة، مع استقبال دفعات `messaging-history.set`; لم يكن هناك بديل سحري لواجهة غير متوفرة. تم تفعيل `syncFullHistory` في إنشاء جلسة Baileys، وأصبح النظام يسجل بوضوح عندما تكون واجهة التاريخ غير متاحة بدلاً من الادعاء بإكمال تاريخ غير مقروء.

## الإصلاحات

| المجال | الإصلاح |
|---|---|
| Live messages | استمرار استقبال `messages.upsert` داخل Backend، مع تسجيل أخطاء الاستقبال بدلاً من إسكاتها |
| History | استمرار استقبال `messaging-history.set`، تفعيل `syncFullHistory` و`shouldSyncHistoryMessage`، وتسجيل عدم توفر واجهة التاريخ |
| Message idempotency | جدول `auto_search_processed_messages` ومفتاح `job_id + account_id + message_id` لمنع إعادة معالجة الرسالة |
| Source idempotency | `source_key` وفهرس فريد لكل job يمنع تكرار مصدر الرابط نفسه |
| Multi-link messages | بقي استخراج جميع الروابط من النص، مع حفظ كل رابط ومصدره منفرداً |
| Provenance | الحساب، المجموعة، اسم المجموعة، Message ID، نوع المصدر، وتفاصيل الرابط محفوظة في المصدر |
| Account telemetry | إضافة آخر Message ID، آخر رسالة، آخر مجموعة، وآخر رابط مع عدادات الرسائل والروابط الجديدة والمكررة |
| Group progress | تحديث `groups_discovered` من عدد المجموعات الفريد المحفوظ بدلاً من تضخيمه في كل دورة |
| History requests | تسجيل `history_requested_at` لكل مجموعة ومنع طلب التاريخ نفسه في كل tick |
| Recovery diagnostics | إظهار أخطاء live/history عبر تحذيرات Backend وأحداث AutoSearch بدلاً من ابتلاعها |

## المسار الذي تم التحقق منه

تصل الرسالة من جلسة Baileys إلى `messages.upsert`، ثم تستدعي `AutoSearchService.ingestMessage`. يُحدد النظام المجموعة والحساب، ويطالب بالرسالة في جدول idempotency، ثم يستخرج النص والروابط، يطبع الرابط ويتحقق من نوعه، يحفظ الرابط ومصدره، يحدث الإحصائيات، ويسجل Event وSocket.IO broadcast. تصل دفعات التاريخ من `messaging-history.set` إلى `ingestHistory` وتستخدم نفس خط التحليل، لذلك لا يوجد مسار تاريخي مختلف يسمح بتكرار غير محمي.

## الاختبارات

تم اجتياز اختبار استخراج رابطين من رسالة واحدة، منع تكرار الرابط داخل الرسالة، تصنيف المجموعة والقناة، تطبيع HTTP إلى HTTPS، رفض الرابط غير الخاص بواتساب، وفحص وجود جدول الرسائل المعالجة وقيد المصدر الفريد. كما اجتازت ملفات AutoSearch وWhatsAppManager فحص الصياغة، واجتاز `git diff --check`، وبُنيت الواجهة Production وظهر chunk `AutoSearchView`.

## حدود التحقق الحي

لا توجد في بيئة التدقيق جلسة WhatsApp متصلة أو PostgreSQL إنتاجية يمكن من خلالها إثبات عدد الرسائل والمجموعات فعلياً أو قياس Recovery بعد فصل الشبكة. لذلك لم يتم الادعاء بأن القراءة الحية أو التاريخ الكامل تم اختباره. بعد النشر يجب تشغيل حساب متصل، تفعيل AutoSearch، إرسال رسالة اختبار في مجموعة، ثم التحقق من `auto_search_processed_messages`, `auto_search_links`, `auto_search_link_sources`, وSocket.IO.

## الملفات المعدلة

تم تعديل `AutoSearchService.js` و`WhatsAppManager.js` وتوسيع `backend/test-auto-search.js`، دون تغيير أقسام الاستيراد أو الكلمات المفتاحية أو مركز الذكاء والأتمتة.
